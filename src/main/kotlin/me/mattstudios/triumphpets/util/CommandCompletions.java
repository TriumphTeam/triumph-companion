package me.mattstudios.triumphpets.util;

import me.mattstudios.triumphpets.TriumphPets;

public class CommandCompletions {

    public static void registerCommandCompletions(TriumphPets plugin) {
        //plugin.getCommandManager().getCompletionHandler().register("pet_types", c -> Arrays.stream(PetType.values()).map(Objects::toString).collect(Collectors.toList()));
    }

}
